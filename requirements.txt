# BlockConnect 依赖

# 核心依赖
asyncio-mqtt>=0.16.1
websockets>=12.0

# 加密支持
cryptography>=42.0.0

# 配置解析
pyyaml>=6.0.1

# 日志
colorlog>=6.8.0

# 可选依赖（用于开发）
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.0.0
flake8>=7.0.0
mypy>=1.8.0

# 文档生成
sphinx>=7.2.0
sphinx-rtd-theme>=2.0.0
